using System.Collections;
using System.Collections.Generic;
using theArch_LD46.GlobalHelper;
using UnityEngine;
using UnityEngine.SceneManagement;

namespace theArch_LD46
{
    /*public class MasterGameManager : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            SceneManager.LoadScene(StaticData.SCENE_ID_ADDITIVE_LV1, LoadSceneMode.Additive);
        }

        // Update is called once per frame
        void Update()
        {
            if (Input.GetButtonDown(StaticData.INPUT_BUTTON_NAME_GAME_START))
            {
                SceneManager.UnloadSceneAsync(StaticData.SCENE_ID_ADDITIVE_LV1);
            }
        }
    }*/
}